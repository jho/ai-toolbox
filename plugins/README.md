# Plugins

Plugin source packages live here.

Current plugins:

- [toolbox-catalog-manager](toolbox-catalog-manager/README.md)

Notes:

- Plugin packages are source artifacts, not automatically installed runtime assets.
- Use the catalog in `catalog/plugins.yaml` to record how each plugin should be installed on Codex
  and Claude.
